# DCS Frontend

Vue 3 + TypeScript + Vite

## Development

```bash
npm install
npm run dev
```

Configuration in `.env.development`

## Production

Built and served by backend Go application. Config injected at runtime via `docker-entrypoint.sh`.
